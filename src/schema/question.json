{
  "$id": "question.json",
  "properties": {
    "type": {
      "type": "string",
      "enum": ["input", "list", "checkbox", "confirm", "number", "rawlist", "expand", "password", "editor"]
    },
    "name": {
      "type": "string"
    }
  },
  "required": ["name"]
}
